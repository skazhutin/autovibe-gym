import { EmptyState } from "../components/ui";

export default function Datasets() {
  return <EmptyState icon="layers" title="Datasets" text="Экран в разработке — будет реализован в следующем коммите." />;
}
