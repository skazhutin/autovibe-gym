import { EmptyState } from "../components/ui";

export default function Models() {
  return <EmptyState icon="layers" title="Models" text="Экран в разработке — будет реализован в следующем коммите." />;
}
