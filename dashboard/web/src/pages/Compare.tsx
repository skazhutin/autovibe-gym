import { EmptyState } from "../components/ui";

export default function Compare() {
  return <EmptyState icon="layers" title="Compare" text="Экран в разработке — будет реализован в следующем коммите." />;
}
