import { EmptyState } from "../components/ui";

export default function NewRun() {
  return <EmptyState icon="layers" title="NewRun" text="Экран в разработке — будет реализован в следующем коммите." />;
}
