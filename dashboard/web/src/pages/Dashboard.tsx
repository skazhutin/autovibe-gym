import { EmptyState } from "../components/ui";

export default function Dashboard() {
  return <EmptyState icon="layers" title="Dashboard" text="Экран в разработке — будет реализован в следующем коммите." />;
}
