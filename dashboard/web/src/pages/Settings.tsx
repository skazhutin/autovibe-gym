import { EmptyState } from "../components/ui";

export default function Settings() {
  return <EmptyState icon="layers" title="Settings" text="Экран в разработке — будет реализован в следующем коммите." />;
}
