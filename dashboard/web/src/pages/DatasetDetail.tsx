import { EmptyState } from "../components/ui";

export default function DatasetDetail() {
  return <EmptyState icon="layers" title="DatasetDetail" text="Экран в разработке — будет реализован в следующем коммите." />;
}
