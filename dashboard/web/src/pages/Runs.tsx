import { EmptyState } from "../components/ui";

export default function Runs() {
  return <EmptyState icon="layers" title="Runs" text="Экран в разработке — будет реализован в следующем коммите." />;
}
