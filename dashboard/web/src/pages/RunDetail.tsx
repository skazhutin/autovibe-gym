import { EmptyState } from "../components/ui";

export default function RunDetail() {
  return <EmptyState icon="layers" title="RunDetail" text="Экран в разработке — будет реализован в следующем коммите." />;
}
