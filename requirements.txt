# LLM client (OpenAI-compatible — works with vLLM, OpenAI, proxies)
openai>=1.40.0
google-genai>=1.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0

# Data & ML
openpyxl>=3.1.0
pandas>=2.0.0
numpy>=1.26.0
cloudpickle>=3.0
# Pinned: candidate models are pickled inside the sandbox and unpickled on the
# host for replay/submit. A version skew (e.g. 1.7 image vs 1.8 host) triggers
# sklearn InconsistentVersionWarning and risks invalid replay. Keep this exact
# pin identical in the sandbox image (Dockerfile.sandbox installs this file).
scikit-learn==1.7.2
xgboost>=2.0.0
lightgbm>=4.0.0

# Visualisation — agents routinely import these for EDA cells; a missing import
# aborts the whole restart_and_run_all clean run, so keep them installed.
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.0.0

# LLM-agent ML toolbox — libraries agents commonly reach for when solving
# tabular tasks. Missing imports crash the executor, so keep them available.
catboost>=1.2.0          # gradient boosting with native categorical support
optuna>=3.0.0            # hyperparameter optimisation (study / trial API)
shap>=0.41.0             # feature importance / explainability
imbalanced-learn>=0.10.0 # SMOTE, class-balance utilities (imblearn.*)
category_encoders>=2.6.0 # target encoding, WOE, etc. (caused ImportError in past runs)
statsmodels>=0.14.0      # OLS, logit, time-series; some agents use it for baselines
tabulate>=0.9.0          # pretty-print DataFrames/tables in notebook output

# Experiment tracking (local MLflow server)
mlflow>=2.14.0

# Real notebook backend
nbformat>=5.10.0
nbclient>=0.10.0
jupyter_client>=8.6.0
ipykernel>=6.29.0

# Tests
pytest>=8.0.0

# Server-side only — install on the H200 machine
# vllm>=0.5.0
